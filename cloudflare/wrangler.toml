name = "jobagent-companion-local"
compatibility_date = "2026-09-19"
pages_build_output_dir = "public"

# This is a local binding declaration only. Creating a remote D1 database is
# intentionally out of scope for this scaffold.
[[d1_databases]]
binding = "DB"
database_name = "jobagent-local"
database_id = "local-only-placeholder"
migrations_dir = "migrations"
